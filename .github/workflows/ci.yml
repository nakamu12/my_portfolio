name: CI

on:
  pull_request:
    branches: [develop/v2, main]
  push:
    branches: [develop/v2]

jobs:
  lint-and-format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install
        working-directory: ./v2

      - name: Lint
        run: bun run lint
        working-directory: ./v2

      - name: Format check
        run: bun run format:check
        working-directory: ./v2

      - name: Type check
        run: bun astro check 2>/dev/null || true
        working-directory: ./v2

      - name: Build
        run: bun run build
        working-directory: ./v2
